spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/framecut
    username: framecut
    password: framecut123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

server:
  port: 8080
  servlet:
    session:
      timeout: 24h

jwt:
  secret: framecutSecretKey2024VeryLongAndSecureKeyForJWTSigningPurpose
  expiration: 86400000

tmdb:
  api-key: 4a6a57353f4c54fcd1506b2e15df190a
  base-url: https://api.themoviedb.org/3
  image-base-url: https://image.tmdb.org/t/p
  fetch-on-startup: true
  pages-per-run: 50

logging:
  level:
    com.framecut: INFO
    org.springframework.security: WARN
